import 'package:flutter/material.dart';

class Systempage extends StatelessWidget {
  const Systempage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("SYSTEM REPAIR AND FIX")),
    );
  }
}
