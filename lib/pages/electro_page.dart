import 'package:flutter/material.dart';

class Electropage extends StatelessWidget {
  const Electropage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("ELECRRO DIAGONSTICS")),
    );
  }
}
