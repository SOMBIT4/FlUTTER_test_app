import 'package:flutter/material.dart';

class Fuelpage extends StatelessWidget {
  const Fuelpage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("FUEL REPAIR SYSTEM")),
    );
  }
}
